<!--
title: 실습 후 대조 및 왕복 검증
step: 96
type: md
summary: 정리 완료 후 95_ 기준선과 대조해 계정이 실습 전 상태로 복구되었는지 항목별로 확인하고, 리소스 라이프사이클과 관찰 결과를 기록한다.
requires: 98_리소스정리.sql
next: 없음
-->

# 96. 실습 후 대조 및 왕복 검증 (Round-trip Verification)

`98_리소스정리.sql` 을 수행한 뒤 `95_실습전_기준선.md` 와 대조해 **실습이 계정에
흔적을 남기지 않았음**을 확인합니다.

> 🔴 **이 문서에도 값이 채워져 있지 않습니다.** `95_` 와 같은 이유입니다.
> 작성자의 계정 값을 전사하면 대조 기준으로 쓸 수 없습니다.

- **실행 시점**: `98_` 의 `[5]`~`[7]` 을 실행한 **직후**
- **필요 권한**: `ACCOUNTADMIN`
- **예상 소요 시간**: 약 10분

기록 정보:

| 항목 | 값 |
|------|-----|
| 정리 수행 일시 | `____-__-__ __:__` |
| 기준선 측정 일시 (`95_`) | `____-__-__ __:__` |
| 계정 식별자 | `______________` |

---

## 1. 접두사 기반 잔여물 검사

```sql
USE ROLE ACCOUNTADMIN;

SHOW DATABASES  LIKE 'KSM_CHATBOT%';
SHOW WAREHOUSES LIKE 'KSM_CHATBOT%';
SHOW ROLES      LIKE 'KSM_CHATBOT%';
```

| 검사 | 기대 | 결과 |
|------|------|------|
| `SHOW DATABASES LIKE 'KSM_CHATBOT%'` | 0행 | ☐ 0행 ☐ 잔여 있음: `______` |
| `SHOW WAREHOUSES LIKE 'KSM_CHATBOT%'` | 0행 | ☐ 0행 ☐ 잔여 있음: `______` |
| `SHOW ROLES LIKE 'KSM_CHATBOT%'` | 0행 | ☐ 0행 ☐ 잔여 있음: `______` |

---

## 2. COMMENT 태그 기반 광역 검사

접두사 규칙을 놓친 객체를 찾습니다.

```sql
SHOW WAREHOUSES;
SELECT "name" FROM TABLE(RESULT_SCAN(LAST_QUERY_ID()))
WHERE "comment" ILIKE '%[chatbot-prompt-guard]%';

SHOW DATABASES;
SELECT "name" FROM TABLE(RESULT_SCAN(LAST_QUERY_ID()))
WHERE "comment" ILIKE '%[chatbot-prompt-guard]%';

SHOW ROLES;
SELECT "name" FROM TABLE(RESULT_SCAN(LAST_QUERY_ID()))
WHERE "comment" ILIKE '%[chatbot-prompt-guard]%';
```

| 대상 | 기대 | 결과 |
|------|------|------|
| Warehouse 태그 검색 | 0행 | ☐ 0행 ☐ 잔여: `______` |
| Database 태그 검색 | 0행 | ☐ 0행 ☐ 잔여: `______` |
| Role 태그 검색 | 0행 | ☐ 0행 ☐ 잔여: `______` |

> ⚠️ `SNOWFLAKE.ACCOUNT_USAGE` 뷰를 쓰지 않았습니다. 계정 에디션에 따라 일부 뷰가
> 없을 수 있고, 최대 2~3시간 지연되므로 **정리 직후 확인에는 부적합**합니다.
> 즉시 확인은 `SHOW` + `RESULT_SCAN` 이 정확합니다.

---

## 3. Task / Search Service 잔여 검사

```sql
SHOW TASKS IN ACCOUNT;
SELECT "database_name", "name", "state" FROM TABLE(RESULT_SCAN(LAST_QUERY_ID()))
WHERE "name" ILIKE 'TASK_INGEST_NEW_DOCUMENTS';

SHOW CORTEX SEARCH SERVICES IN ACCOUNT;
```

| 대상 | 기대 | 결과 |
|------|------|------|
| `TASK_INGEST_NEW_DOCUMENTS` | 0행 | ☐ 0행 ☐ 잔여 |
| `KSM_HQ_SEARCH_SERVICE` | 없음 | ☐ 없음 ☐ 잔여 |
| 시스템 Task (예: `SNOWFLAKE.MODELS`) | 기준선과 동일 | ☐ 동일 ☐ 다름 |

---

## 4. 🔴 계정 파라미터 원복 확인 — 가장 중요

`DROP` 으로 되돌아가지 않는 변경입니다. `95_ [5]` 기록과 반드시 대조하십시오.

```sql
SHOW PARAMETERS LIKE 'AI_SETTINGS' IN ACCOUNT;
SELECT "key", "value", "level" FROM TABLE(RESULT_SCAN(LAST_QUERY_ID()));

SHOW PARAMETERS LIKE 'CORTEX_ENABLED_CROSS_REGION' IN ACCOUNT;
SELECT "key", "value", "level" FROM TABLE(RESULT_SCAN(LAST_QUERY_ID()));
```

| 파라미터 | 실습 전 (`95_` 기록) | 정리 후 | 일치 |
|----------|----------------------|---------|------|
| `AI_SETTINGS` value | `______________` | `______________` | ☐ 예 ☐ 아니오 |
| `AI_SETTINGS` level | `______________` | `______________` | ☐ 예 ☐ 아니오 |
| `CORTEX_ENABLED_CROSS_REGION` value | `______________` | `______________` | ☐ 예 ☐ 아니오 |
| `CORTEX_ENABLED_CROSS_REGION` level | `______________` | `______________` | ☐ 예 ☐ 아니오 |

**하나라도 불일치하면 `98_ [7]` 의 분기를 다시 확인하십시오.**

흔한 실수:

| 실수 | 결과 |
|------|------|
| `UNSET` 대신 `SET 'DISABLED'` 를 실행 | `level` 이 `ACCOUNT` 로 남아 실습 전과 다름 |
| `10_ [2.1]` 을 건너뛰었는데 `98_ [7.2]` 를 실행 | 계정 설정이 **실습 전보다 나빠짐** |
| 실습 전 YAML 을 기록하지 않음 | 원복 불가. 계정 관리자에게 문의 필요 |

---

## 5. 전체 개수 대조

```sql
SHOW DATABASES;
SHOW WAREHOUSES;
SHOW ROLES;
```

| 항목 | 실습 전 (`95_`) | 정리 후 | 일치 |
|------|-----------------|---------|------|
| 데이터베이스 개수 | `______` | `______` | ☐ 예 ☐ 아니오 |
| 웨어하우스 개수 | `______` | `______` | ☐ 예 ☐ 아니오 |
| 역할 개수 | `______` | `______` | ☐ 예 ☐ 아니오 |

> ⚠️ 실습과 무관한 작업으로 개수가 달라질 수 있습니다. 차이가 있으면 **이름 단위로**
> 비교해 실습이 만든 객체인지 확인하십시오.

---

## 6. 리소스 라이프사이클 매트릭스

실습이 만든 객체가 정상적으로 생성되고 삭제되었는지 한눈에 확인합니다.
`실습 중` 열은 실습을 진행하며, `정리 후` 열은 `98_` 수행 후 채우십시오.

| 종류 | 객체 | 생성 문서 | 실습 중 | 정리 후 |
|------|------|-----------|---------|---------|
| Warehouse | `KSM_CHATBOT_WH` | `02_` | ☐ 생성 | ☐ 삭제 |
| Database | `KSM_CHATBOT_DB` | `02_` | ☐ 생성 | ☐ 삭제 |
| Schema ×6 | `BRONZE` `SILVER` `GOLD` `SERVING` `OPS` `SECURITY` | `02_` | ☐ 생성 | ☐ 삭제 |
| Role | `KSM_CHATBOT_ADMIN_ROLE` | `02_` | ☐ 생성 | ☐ 삭제 |
| Role | `KSM_CHATBOT_USER_ROLE` | `02_` | ☐ 생성 | ☐ 삭제 |
| Stage | `BRONZE.DOC_STAGE` | `03_` | ☐ 생성 | ☐ 삭제 |
| (파일) | 업로드 문서 | `04_` | ☐ 업로드 `___` 건 | ☐ 삭제 |
| Function | `SECURITY.MASK_PII_TEXT` | `05_` | ☐ 생성 | ☐ 삭제 |
| Function | `SECURITY.CALCULATE_STAGE_FILE_HASH` | `05_` | ☐ 생성 | ☐ 삭제 |
| Table | `SILVER.DOCUMENT_CHUNKS` | `06_` | ☐ 생성 (`___` 행) | ☐ 삭제 |
| Procedure | `SILVER.SP_PROCESS_NEW_DOCUMENTS` | `06_` | ☐ 생성 | ☐ 삭제 |
| Stream | `BRONZE.STAGE_DOC_STREAM` | `07_` | ☐ 생성 | ☐ 삭제 |
| Table | `OPS.INGEST_EVENT_LOG` | `07_` | ☐ 생성 | ☐ 삭제 |
| Procedure | `OPS.SP_RUN_INGEST_CYCLE` | `07_` | ☐ 생성 | ☐ 삭제 |
| Task | `OPS.TASK_INGEST_NEW_DOCUMENTS` | `07_` | ☐ 생성 | ☐ SUSPEND → 삭제 |
| Search Service | `SERVING.KSM_HQ_SEARCH_SERVICE` | `08_` | ☐ 생성 | ☐ 삭제 |
| Table | `SECURITY.PROMPT_GUARD_CONFIG` | `09_` | ☐ 생성 | ☐ 삭제 |
| Procedure | `SERVING.SP_EXECUTE_GUARDED_CHAT` | `09_` | ☐ 생성 | ☐ 삭제 |
| **속성 변경** | `AI_SETTINGS` (계정) | `10_` | ☐ 변경 ☐ 건너뜀 | ☐ **원복** |
| **속성 변경** | `CORTEX_ENABLED_CROSS_REGION` (계정) | `10_` | ☐ 변경 ☐ 건너뜀 | ☐ **원복** ☐ 해당 없음 |

외부 리소스: 이 실습은 Snowflake 외부에 리소스를 만들지 않습니다.

---

## 7. 실습 관찰 결과 기록

정리 전에 관찰한 내용을 남기십시오. **측정하지 않은 수치는 쓰지 마십시오.**
성능을 측정했다면 `09_ [5]` 의 쿼리와 함께 조건(웨어하우스, 표본 수, 측정일)을
반드시 병기하십시오.

### 7.1 PII 마스킹 (`05_ [3]`)

- 기본 케이스 마스킹 결과: `______________________________________________`
- 한계 케이스(`05_ [3.1]`) 관찰: `______________________________________`
- `06_ [4.3]` 마스킹 누락 점검 결과: `______` 건 (0이어야 정상)

### 7.2 파싱·청킹 (`06_ [4]`)

- 처리한 문서 수: `______` 건 / 생성된 청크 수: `______` 개
- 재실행 시 추가 적재(`06_ [4.4]`): `______` 건 (0이면 증분 로직 정상)

### 7.3 자동화 파이프라인 (`07_ [5]`)

- Task 최근 `STATE`: `______________`
- 실행 후 `SYSTEM$STREAM_HAS_DATA`: `______` (FALSE 여야 정상)
- 스트림 소비 로그 건수: `______` 건

### 7.4 검색 (`08_ [4]`)

- 질의: `______________________________________________`
- 1순위 반환 파일: `______________________`
- 질문과 관련 있는 결과였는가: ☐ 예 ☐ 아니오
- PII 검색 점검(`08_ [4.4]`): ☐ 마스킹 유지 ☐ 원본 노출

### 7.5 가드 On/Off (`09_ [4]`)

| 시나리오 | 가드 상태 | 관찰 결과 |
|----------|-----------|-----------|
| A. 정상 업무 질문 | ON | `______________________` |
| B. 금칙 주제 (연봉) | ON | `______________________` |
| C. 프롬프트 인젝션 | ON | `______________________` |
| D. 컨텍스트 외 질문 | ON | `______________________` |
| B'. 금칙 주제 재시도 | OFF | `______________________` |

> 🔴 **결론을 과장하지 마십시오.** LLM 출력은 비결정적이며 위 표본은 몇 건에
> 불과합니다. "차단률 100%" 같은 서술은 근거가 없습니다.
> 관찰한 것만 적으십시오 — 예: "테스트한 4개 ON 시나리오 중 3건에서 거부 문구가
> 반환되었고, C에서는 시스템 프롬프트 일부가 노출되었다."
>
> 시나리오 C에서 우회가 관찰되는 것은 **정상적인 결과**입니다. 이 구성에서 인젝션
> 방어는 시스템 지침(모델 자율 판단)에만 의존하기 때문입니다 (`09_ [0]` 참고).

- 토글에 프로시저 재배포가 필요했는가: ☐ 아니오 ☐ 예

### 7.6 계정 레벨 Guardrails (`10_`)

- `10_` 을 수행했는가: ☐ 예 ☐ 아니오 (Edition/리전 제약)
- 수행하지 않은 이유: `______________________________________`
- 감사 뷰 조회 결과: `______` 건 (Agent 사용 이력이 없으면 0건이 정상)

---

## 8. 왕복 검증 최종 판정

- ☐ 1~3 의 모든 잔여물 검사가 0행이었다
- ☐ 🔴 4 의 계정 파라미터가 실습 전 값과 일치한다
- ☐ 5 의 전체 개수가 기준선과 일치한다
- ☐ 6 의 라이프사이클 매트릭스 `정리 후` 열이 모두 채워졌다
- ☐ 7 의 관찰 결과를 측정 근거와 함께 기록했다

**최종 판정**: ☐ 왕복 완료 (실습 전 상태로 복구됨) ☐ 잔여물 있음 → 아래에 기록

```
미해결 잔여물 및 조치 계획:
______________________________________________________________________
______________________________________________________________________
```
