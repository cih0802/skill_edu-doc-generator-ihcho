# 교육자료 카탈로그

워크스페이스의 교육자료 목록과 **명명 규칙의 정본**이다.
새 교육자료를 만들 때마다 이 문서에 한 행을 추가한다.

- **최종 갱신:** 2026-09-17 (3자료 교차 감사 · 도메인 스킬 대조 · EDU-02 완주 재검증)

---

## 1. 명명 규칙

```
/workspace/EDU-<NN>_<topic-slug>/
```

| 요소 | 규칙 |
| :--- | :--- |
| `EDU-` | 교육자료 폴더 식별 접두사. 폴더 내부 번호(`00_`~`99_`)와 시각적으로 구분하기 위해 붙인다 |
| `<NN>` | **생성 순번.** 배정 후 **절대 변경하지 않는다.** 두 자리, 100번째부터 세 자리 |
| `<topic-slug>` | 영문 소문자 + 하이픈. 스킬 명세의 `TOPIC_SLUG` 정의와 동일 |

### 🔴 번호에 분류를 넣지 않는다

번호는 **불변 식별자**이며 분야·난이도·순서를 뜻하지 않는다. 이유:

- **한 자료가 여러 분야에 걸친다.** 아래 표를 보면 현재 3개 자료 **모두** 파이프라인을
  포함한다. 대표 분야를 하나 골라 번호에 넣으면 임의적이고 오해를 만든다
- **분야는 나중에 바뀐다.** 번호에 분류가 있으면 재번호가 필요해지고,
  재번호는 외부 참조(문서 링크, 수강 안내, 티켓)를 깨뜨린다
- 스킬이 폴더 **내부** 번호에 대해 이미 같은 원칙을 둔다 —
  "번호는 오름차순·유일하되 연속일 필요 없다", "재번호는 외부 참조를 깨뜨린다"

분야는 아래 **분야 태그** 열에서 **다중으로** 관리한다. 태그는 자유롭게 추가·수정한다.

### 폴더명은 영문 slug, 한글 제목은 이 카탈로그에서

경로·CLI·git 에서 안전하도록 폴더명은 영문 slug 로 둔다.
사람이 읽는 한글 제목은 이 카탈로그와 각 자료의 `01_교육자료_정리본.md` 제목에서 제공한다.

### 번호 배정 절차

1. 이 카탈로그에서 가장 큰 `NN` 을 찾는다
2. `NN + 1` 을 배정한다. **빈 번호를 재사용하지 않는다** (폐기된 자료의 번호도 비워 둔다)
3. 폴더를 만들고 이 표에 행을 추가한다

---

## 2. 교육자료 목록

| 번호 | 한글 제목 | 폴더 | 분야 태그 (다중) | 문서 | 규격 | 실행 검증 |
| :---: | :--- | :--- | :--- | :---: | :---: | :--- |
| **EDU-01** | Openflow CDC 증분 동기화 실습 | `EDU-01_openflow-cdc/` | `데이터통합` `파이프라인` `네트워크` `CDC` | 15 | ✅ | ⚠️ **부분 완주 (도달 한계 확인)** — Deployment·Runtime·커넥터까지 실제 실행 + 정리·대조 완료. **트라이얼 계정에서 EAI 차단**으로 CDC 적재는 불가. 9차에서 `95_`/`96_` 신설 및 정리 방어 블록 추가. 상세는 `01_` 9차 |
| **EDU-02** | 챗봇 프롬프트 가드제한 (RAG 보안) | `EDU-02_chatbot-prompt-guard/` | `AI/RAG` `보안` `파이프라인` `거버넌스` `CortexSearch` | 17 | ✅ | ✅ **완주 (2026-09-17 재완주)** — 한국어 PDF 로 엔드투엔드 + 정리 2회 실행 + 잔여물 0. **모델 수명·임베딩 언어 지원·AI_COMPLETE 전환** 반영. 실행 중 결함 2건 추가 발견·정정. 상세는 `01_` 4차 |
| **EDU-03** | SSO 및 사용자 권한 동기화 | `EDU-03_sso-user-sync/` | `인증/ID` `보안` `파이프라인` `SCIM` `OAuth` `운영` | 16 | ✅ | ✅ **완주** — 02_~08_ 전량 실행 + `98_` 정리·대조 완료(2~4차). 5차에서 동적 SQL 게이트·감사 로깅 결함 정정 후 해당 부분 집중 재검증. 상세는 `01_` 5차 |

### 열 설명

- **규격**: 스킬 문서 규격(`00_index.md` / `01_` 정리본 / `98_` 정리 / `99_` 인덱스 / 메타 주석 / `requires`·`next` 체인) 준수 여부
- **실행 검증**: 실습을 실제로 완주했는지. 기준은 스킬 품질 기준의 "완주 검증" 절
  (순차 실행 → 결과 쿼리 확인 → `98_` 정리 실행 → 사전 스냅샷 대조)

---

## 3. 분야 태그 사전

표기를 일관되게 유지하기 위한 목록이다. 필요하면 추가한다.

| 태그 | 범위 |
| :--- | :--- |
| `데이터통합` | Openflow, 커넥터, 외부 소스 적재 |
| `파이프라인` | Stream, Task, Dynamic Table, 증분 처리 |
| `AI/RAG` | Cortex AI 함수, 임베딩, 청킹, 검색 기반 생성 |
| `CortexSearch` | 검색 서비스 생성·인덱싱·서빙 |
| `보안` | 마스킹, 가드레일, 금칙, 최소 권한 |
| `거버넌스` | RBAC, 소유권, 태그, 정책 |
| `인증/ID` | SSO, SAML, SCIM, OAuth, 사용자 프로비저닝 |
| `네트워크` | Network Rule, EAI, PrivateLink, 방화벽 |
| `운영` | 모니터링, 감사, 비용, 스케줄링 |
| `CDC` | 변경 데이터 캡처 |

---

## 4. 현재 미해결 항목

| 항목 | 내용 |
| :--- | :--- |
| **🔴 Cortex 모델 수명 — 시간이 지나면 자동으로 결함이 된다** | **모든 AI 실습 자료에 반복 적용되는 항목이다.** Cortex 모델은 `GA → LEGACY → EOL` 로 폐기되고, **LEGACY 날짜가 지나면 직전 30일 내 미사용 계정에서는 선택 자체가 불가능**해진다. 즉 자료를 고치지 않아도 **가만히 있으면 실습이 실패로 바뀐다.** 2026-09-17 실측으로 EDU-02 의 `llama3.1-70b` 가 이 상태였다(LEGACY, legacy_date 2026-08-12, eol_date No sooner than 2026-10-14). EDU-02 `02_ [0.6]` 에 `SHOW CORTEX BASE MODELS` 게이트를 넣어 학습자가 실습 전에 스스로 확인하게 했다. **AI 자료를 새로 만들 때 이 게이트를 반드시 포함할 것.** 임베딩 모델은 수명과 별개로 **언어 지원**(English-only vs Multilingual)도 확인해야 한다 — 한국어 코퍼스에 English-only 모델을 쓰면 오류 없이 검색 품질만 나빠진다 |
| EDU-01 CDC 적재 미검증 | **계정 제약이며 자료 결함이 아니다.** 트라이얼 계정은 External Access Integration 이 차단되어(`External access is not supported for trial accounts`) 외부 소스에 연결할 수 없다. 완주하려면 ① 외부에서 접근 가능한 PostgreSQL 과 ② 외부 액세스가 활성화된 계정(유료 전환 또는 담당자 승인)이 모두 필요하다 |
| 인증 왕복 미검증 (EDU-03) | SAML·SCIM·OAuth 인티그레이션은 **생성만** 검증되었다. 인증서·공개키가 더미이고 Entra ID 테넌트가 없어 실제 로그인·JWT·프로비저닝 왕복은 검증되지 않았다. Snowflake 외부 요건이다 |
| 태스크 자연 발동 미검증 | EDU-01·EDU-02·EDU-03 모두 `EXECUTE TASK` 로 트리거해 확인했다. 스케줄(1초·5분·자정) 도달 전에 정리했다 |
| EDU-02 Cortex Guard 실동작 미관찰 | `{'guardrails': TRUE}` 가 실제로 유해 응답을 필터한 사례는 관찰하지 못했다. 관찰된 차단은 모두 시스템 지침(계층 2)이 수행했다. Cortex AI Guardrails(계정 파라미터)는 적용 대상이 CoCo/CoWork/Cortex Agents 이며 이 실습은 Agent 를 만들지 않는다 |

### 3자료 교차 감사에서 확인된 반복 패턴 (다음 작업자를 위한 메모)

2026-09-17 감사에서 **세 자료에 공통으로 나타난 결함 유형**이다. 새 자료를 만들거나
개선할 때 이 목록을 먼저 훑으면 같은 결함을 반복하지 않는다.

| 패턴 | 실측 사례 | 어떻게 잡는가 |
| :--- | :--- | :--- |
| **`IF EXISTS` 가 상위 DB 부재를 막지 못한다** | EDU-02 `98_` 2회차 실행이 첫 구문부터 실패 (`Database … does not exist`). EDU-01 도 같은 구조였다 | 정리를 **반드시 2회 실행**한다. `SHOW DATABASES LIKE` + `RESULT_SCAN` 방어 블록으로 감싼다 |
| **단위 테스트가 실제 입력 형태와 다르다** | EDU-02 PII 마스킹이 단위 테스트는 통과하는데 `AI_PARSE_DOCUMENT` 출력(`010- 9876- 5432`)에서는 **전혀 마스킹되지 않았다** | 테스트 입력을 **실제 파이프라인이 받는 형태**로 만든다. 파싱 결과를 눈으로 보고 그 문자열로 시험한다 |
| **`EXCEPTION` 핸들러 자체가 실패한다** | EDU-03 이 `VALUES (…, SQLERRM)` 로 콜론을 빼서 감사 로그 INSERT 가 실패 → "실패를 기록한다"는 설계가 무효 | 예외를 **의도적으로 일으켜** 핸들러를 실행해 본다. 정상 경로 실행으로는 절대 드러나지 않는다 |
| **핸들러의 사각지대** | EDU-03 `DECLARE` 절 커서의 테이블 부재는 `BEGIN` 전에 해석되어 핸들러가 잡지 못한다 | 감사 테이블만 보지 말고 `TASK_HISTORY.ERROR_MESSAGE` 도 확인하도록 자료에 명시한다 |
| **한 자료 안에서 방어 수준이 불균등하다** | EDU-03 `SP_SYNC` 에는 RLIKE 식별자 게이트가 있는데 `SP_AUDIT` 에는 없었다 | 같은 위험 패턴(동적 SQL, 예외 처리)을 **폴더 전체에서 grep 해 전수 대조**한다 |
| **문서 간 서술이 갈라진다** | EDU-01 `03_` 정리 주석이 Deployment `TERMINATE` 를 빼먹어 `98_` 과 모순. EDU-03 `90_` 이 재번호 이전 파일명을 참조 | 같은 절차를 두 곳에 복제하지 않는다. 한쪽을 **정본으로 지정하고 다른 쪽은 참조**만 한다 |
| **쓰지 않는 계정 레벨 권한을 위임한다** | EDU-01 `GRANT CREATE COMPUTE POOL` (Compute Pool 생성 0건). EDU-03 계정 권한 6개가 대장 미등재 | `grep "GRANT .* ON ACCOUNT"` 로 전수 추출해 **실제 사용처와 대조**하고 대장에 등재한다 |
| **총괄 합격 선언이 되살아난다** | EDU-03 `90_` 에 `모든 스크립트는 … 검증이 완료되었습니다` 가 잔존 (배너로 정정했으나 본문은 그대로) | 정정할 때 **배너를 붙이는 것으로 끝내지 말고 본문 문장을 지운다** |

이전에 등재되어 있던 아래 항목은 **모두 해소되었다.**

| 해소된 항목 | 결과 |
| :--- | :--- |
| ~~EDU-03 규격 미준수~~ | ✅ 4차 개선으로 규격 정렬 완료 (`00_`/`01_`/`98_`/`99_`, 메타 주석, `requires`·`next`) |
| ~~EDU-03 결함 2건 미정정~~ | ✅ 스트림 미소비·`DELETE` 오프보딩 코드 정정 + 실행 재검증 완료 |
| ~~EDU-01 실행 검증 부분~~ | ✅ 완주 시도로 도달 한계를 확정하고 결함 4건을 발견·정정 |
| ~~정리 대기 폴더 5개~~ | ✅ 전량 제거 완료 (§5) |
| ~~EDU-01 `95_`/`96_` 미보유 (3자료 규격 불일치)~~ | ✅ 9차 개선으로 신설. `98_ PART E` 는 `96_` 참조로 축약해 drift 방지 |
| ~~EDU-01 `98_` 재실행 안전성 미조치 (8차 등재)~~ | ✅ 9차 개선으로 방어 블록 코드 추가 + 실행 검증 |
| ~~EDU-01 `08_`/`09_`/`10_` 정리 주석 누락~~ | ✅ 9차 개선으로 추가. `10_` 은 MySQL 소스 정리 절 신설 |
| ~~EDU-02 레거시 모델·English-only 임베딩·구 AI 함수~~ | ✅ 4차 개선으로 교체 + 수명 게이트 신설 + 완주 재검증 |
| ~~EDU-03 동적 SQL 게이트 불균등·감사 로깅 무효~~ | ✅ 5차 개선으로 정정 + 주입 시도 거부를 실행 검증 |

---

## 5. 정리 완료 — 중복·손상 폴더 제거 이력

폴더 이름을 바꾸는 과정에서 생긴 중복본과 손상본을 **전량 제거했다. 잔여물 0.**

- **최종 확인:** 2026-09-16. 워크스페이스 전체 117개 파일 스캔 결과 **읽기 불가 0건**
- 현재 워크스페이스에는 `EDU-01` / `EDU-02` / `EDU-03` 정본 3개와
  `00_SKILL_정본/`, 이 카탈로그만 남아 있다

### 제거 완료

| 폴더 | 제거 방식 | 근거 |
| :--- | :--- | :--- |
| `edu-doc-generator-ihcho/` | `rmdir` (빈 폴더) | 내용 없음 |
| `SSO 및 사용자 권한 동기화 가이드/` | `rmdir` (빈 폴더) | 내용 없음 |
| `챗봇 프롬프트 가드제한 교육자료/` | `rmdir` (빈 폴더) | 내용 없음 |
| `openflow-cdc/` | 이전 세션에서 소멸 | 파일 8개 읽기 불가 손상본. 정본이 더 크고 온전함 |
| `챗봇 프롬프트 가드제한 가이드/` | 이전 세션에서 소멸 | 읽을 수 있는 파일 0개 |
| `openflow-cdc 교육자료/` | 파일 삭제 → `rmdir` | 정본과 비교해 **차이 7건이 모두 이번 세션의 개선분**임을 확인. 개선 전 상태는 `EDU-01_openflow-cdc/_archive/20260916/` 에 보존 |
| `챗봇 프롬프트 금지어 가드 교육자료/` | 파일 삭제 → `rmdir` | 정본과 **전체 재귀 `diff` 차이 0건** |
| `SSO 및 사용자 권한 동기화 교육자료/` | 파일 삭제 → `rmdir` | 정본의 구 판. 차이 4건이 모두 이번 세션의 결함 정정분. 원본은 `EDU-03_sso-user-sync/_archive/20260916/` 에 보존 |

### 이 스테이지 파일시스템 실측 (다음 작업자를 위한 메모)

| 동작 | 결과 |
| :--- | :--- |
| 디렉터리 `mv` | ❌ `Input/output error`. 폴더 이동·이름 변경 불가 |
| 파일 단위 `mv` / `cp` | ✅ 동작 |
| `rm -rf <중첩 디렉터리>` | ❌ `Directory not empty` 로 부분 실패 |
| 회피책 | `find -type f -exec rm -f` → `find -depth -type d -exec rmdir` 순서로 처리 |
| 셸 추가 리다이렉트 `>> 파일` | ❌ `Operation not supported`. **조용히 실패**하므로 내용으로 확인해야 한다 |
| 회피책 | `/tmp` 에서 전체 내용을 만든 뒤 `cp` 로 덮어쓴다 |
| `$(find ...)` 워드 분할 | 공백 포함 파일명이 깨진다. `-print0` + `read -d ''` 를 쓸 것 |

---

## 6. 아카이브 규칙

각 교육자료의 개선 전 원본은 폴더 안에 보존한다. 삭제하지 않는다.

```
EDU-<NN>_<slug>/_archive/<YYYYMMDD>/ARCHIVE_MANIFEST.md
```

| 자료 | 아카이브 | 회차 |
| :--- | :--- | :--- |
| EDU-01 | `_archive/20260916/` (7종) | 완주 검증 전 |
| EDU-02 | `_archive/20260916/` (14종) | 규격 재구성 전 |
| EDU-03 | `_archive/20260916/` (14종) | 규격 재구성 전 |

매니페스트에는 **대체 매핑**과 **그 원본에 남아 있는 결함 목록**을 적는다.
`_archive/` 는 `99_generate_index.py` 의 인덱스 대상에서 제외된다.

> ⚠️ **대체본이 확정되지 않은 문서는 아카이브하지 않는다.** 활성 세트에 대체본이
> 없는 상태로 원본을 옮기면 그 내용이 사라진다.
